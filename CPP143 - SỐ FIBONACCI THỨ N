#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        long long a[1000];
	      a[0]=0, a[1]=1;
	      for (int i=2; i<=92; i++) a[i]=a[i-1]+a[i-2];
          cout << a[n] << "\n";
    }
}
